# environment.yaml
---
name: geo-pipeline
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - gdal
  - rasterio
  - libgdal-jp2openjpeg
  - pytorch
  - torchvision
  - pip
  - pip:
      - kafka-python
      - psycopg2-binary
      - python-dotenv
      - requests
      - websockets
      - pydantic
      - loguru
      - pytest
      - pyyaml
      - minio
      - shapely